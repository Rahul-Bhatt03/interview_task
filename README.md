# Dashboard Web Application

This is a React + Redux dashboard-style web application.

## Setup and Run

1. Clone the repository:
git clone <repo-url>

2. Navigate into the project directory:
cd frontend

3. Install dependencies:
npm install

4. Run the development server:
npm run dev
